import { DashboardPagePlaceholder } from "@/components/dashboard/dashboard-page-placeholder"

export default function AnalyticsPage() {
  return (
    <DashboardPagePlaceholder
      title="Analytics"
      description="Track memory entries written, RAG queries, and whether retrieved context helped the agent—so you can tune prompts and prove trace is improving decisions."
    />
  )
}
