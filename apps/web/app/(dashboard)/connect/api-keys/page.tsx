import { DashboardPagePlaceholder } from "@/components/dashboard/dashboard-page-placeholder"

export default function ConnectApiKeysPage() {
  return (
    <DashboardPagePlaceholder
      title="API keys"
      description="Create and revoke keys for programmatic access (MCP and HTTP APIs). Keys will be scoped to your account and projects once the backend is wired."
    />
  )
}
