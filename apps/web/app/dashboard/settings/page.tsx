import { DashboardPagePlaceholder } from "@/components/dashboard/dashboard-page-placeholder"

export default function SettingsPage() {
  return (
    <DashboardPagePlaceholder
      title="Settings"
      description="Account profile, notifications, and billing when tiers go live. Session and identity are handled by your existing auth setup."
    />
  )
}
